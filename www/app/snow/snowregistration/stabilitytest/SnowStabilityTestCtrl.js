angular
    .module('RegObs')
    .controller('SnowStabilityTestCtrl', function SnowStabilityTestCtrl($scope) {
        var vm = this;

        $scope.$on('$ionicView.loaded', function () { });
    });
