angular
    .module('RegObs')
    .controller('MapDownloadCtrl', function ($scope, $rootScope, $http, $state, $cordovaInAppBrowser, AppSettings, LocalStorage, ObsLocation, Registration, User, Utility, HeaderColor, RegobsPopup, AppLogging) {
        var vm = this;

    });